# ---------------------------------------------------------------------
# SOLVER — excluded until v2.0.0 (published on acceptance).
# v1.0.0 contains data, configurations, results and validation scripts.
# v1.1.0 adds the external mixed-variable NSGA-II reference point
# (nsga2_reference/), which is Python and carries no embargo.
# The MATLAB solver is still excluded here; remove this block when
# adding it in v2.0.0.
# ---------------------------------------------------------------------
dms_si_mix.m
dms_si_mix_*.m
search_step.m
src/

# ---------------------------------------------------------------------
# OS and cloud-sync artifacts
# ---------------------------------------------------------------------
desktop.ini
Thumbs.db
ehthumbs.db
.DS_Store

# ---------------------------------------------------------------------
# MATLAB
# ---------------------------------------------------------------------
*.asv
*.m~
slprj/

# ---------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------
__pycache__/
*.pyc
